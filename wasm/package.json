{
  "author": "Thomas L'Anglais",
  "name": "wasm",
  "version": "0.1.0",
  "scripts": {
    "deploy": "wasm-pack build --release --no-typescript --out-name \"tlang-wasm\" && node scripts/build.js pack",
    "release": "wasm-pack build --release --target web --out-name \"tlang-wasm\" --features wee_alloc",
    "build": "rimraf dist pkg && wasm-pack build --target web --out-name \"tlang-wasm\" && node scripts/build.js debug",
    "start": "rimraf dist pkg && wasm-pack build --target web --out-name \"tlang-wasm\" && webpack-dev-server --config config/debug.config.js",
    "test": "cargo test && wasm-pack test --headless"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^5.0.3",
    "rimraf": "^3.0.0",
    "webpack": "^5.71.0",
    "webpack-cli": "^4.9.2",
    "webpack-dev-server": "^4.7.4"
  }
}
